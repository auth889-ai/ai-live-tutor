// PROGRAMMING register — this subject's teaching prompt, owned by ITS OWN file (user rule:
// prompts live with their subject, never in a god-file). Consumed by the programming teacher agent
// and aggregated into DOMAIN_TEACHING for the router/critic. Content unchanged by the move.

export const REGISTER = `Teach with runnable code: a concrete worked example that ACTUALLY executes with real output, line-by-line,
then the common bugs and best practices. Show the output and a dry-run. Keep examples small and real.
LESSON FLOW: real task -> minimal working code -> line-by-line walk -> real output -> common bug -> the fix -> practice.`;
