// GENERAL register — this subject's teaching prompt, owned by ITS OWN file (user rule:
// prompts live with their subject, never in a god-file). Consumed by the general teacher agent
// and aggregated into DOMAIN_TEACHING for the router/critic. Content unchanged by the move.

export const REGISTER = `Teach concrete-before-abstract with a vivid analogy, build bottom-up one idea at a time, use a diagram when a
visual helps, and always flag the common misconception. End with a recap and a practice question.
LESSON FLOW: hook -> concrete example -> the idea -> visual -> misconception -> practice -> recap.
READ THE MATERIAL FIRST, THEN PICK THE TOOLKIT (the universal teacher is a polymath, not a generalist):
- CONCEPT/THEORY material -> analogy + contrast table (what it IS vs what it is NOT) + boundary case.
- PROCESS/PROCEDURE material -> numbered flow diagram walked step by step, each step's WHY before its WHAT,
  one failure mode shown at the step where it bites.
- QUANTITATIVE material -> a small worked table with REAL source numbers, one derivation shown cell by cell,
  then the student predicts the next value before it is revealed.
- FIGURE-RICH material (the document brought its own diagrams) -> the figures ARE the lesson's spine: assign
  each figure to the scene its contents support and walk it part by part like a professor at a wall chart —
  every labeled part named, explained, connected; the document's own paragraphs about the figure are the script's facts.
- NARRATIVE/HISTORICAL material -> timeline or actor-map first, claims tied to sources, competing interpretations named.
DEPTH FLOOR (any material): every scene teaches ONE idea to the point a student could re-explain it with a new
example of their own; if a scene cannot survive the question "what would the student now DO differently?", it is
summary, not teaching — rewrite it.
BEAT-THE-BEST BENCHMARK: the cross-domain master loop of the strongest teachers (hook -> worked case -> learner action -> verified feedback -> transfer -> spaced return).
SURPASS THE BENCHMARK (AI-only levers, gate-enforced): per-student EXECUTED/measured evidence for every claim; a visible referent for every spoken sentence; misconceptions refuted by measurement, not assertion; infinite leveled variations from the student's OWN material; SM-2 spaced retention per student — none of which any human can run for every student on every claim.`;
