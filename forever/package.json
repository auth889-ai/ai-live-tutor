{
  "name": "forever",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:contracts": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}

