# E2E Tests

Playwright tests for the product experience.

Run after installing Playwright:

```bash
cd forever
npm install
npx playwright test
```

The local static server should serve `apps/web` on port `4177`.

