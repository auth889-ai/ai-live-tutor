# API Tests

```text
tests/
  unit/          pure module tests
  integration/   FastAPI endpoint tests
  fixtures/      reusable input payloads
```

Run:

```bash
cd forever/apps/api
PYTHONPATH=src pytest
```

